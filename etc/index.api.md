## API Report File for "@gfargo/git-scenarios"

> Do not edit this file. It is a report generated by [API Extractor](https://api-extractor.com/).

```ts

import { SimpleGit } from 'simple-git';

// @public
export function abortCherryPick(): Step;

// @public
export function abortMerge(): Step;

// @public
export function abortRebase(): Step;

// @public
export function abortRevert(): Step;

// @public
export function addCommit(opts: {
    message: string;
    files?: FileMap;
    date?: string;
}): Step;

// @public
export function addNote(message: string, options?: {
    ref?: string;
    namespace?: string;
}): Step;

// @public
export function addRemote(name: string, url: string): Step;

// @public
export function addSubmodule(opts: {
    path: string;
    setup: Step;
    branch?: string;
    url?: string;
}): Step;

// @public
export function addWorktree(path: string, options?: {
    branch?: string;
    checkout?: string;
    detach?: boolean;
    from?: string;
}): Step;

// @public
export const allScenarios: readonly Scenario[];

// @public
export function amendCommit(options?: {
    message?: string;
    date?: string;
}): Step;

// @public
export function appendNote(message: string, options?: {
    ref?: string;
    namespace?: string;
}): Step;

// @public
export function applyStash(options?: {
    ref?: string;
}): Step;

// @public
export function assertRepo(source: SnapshotSource): RepoAssertion;

// @public
export type AuthorIdentity = {
    name: string;
    email: string;
    date?: string;
};

// @public
export function bisectStep(verdict: 'good' | 'bad' | 'skip'): Step;

// @public
export const branchAheadOfUpstreamScenario: Scenario;

// @public
export const branchBehindUpstreamScenario: Scenario;

// @public
export const branchDivergedScenario: Scenario;

// @public
export const branchSyncShowcaseScenario: Scenario;

// @public
export const branchTrackingUpstreamScenario: Scenario;

// @public
export function bulkCommits(specs: BulkCommitSpec[]): Step;

// @public
export type BulkCommitSpec = {
    message: string;
    files?: FileMap;
    date?: string;
};

// @public
export function cacheRoot(): string;

// @public
export function chain(...steps: Step[]): Step;

// @public
export function checkoutBranch(name: string): Step;

// @public
export function cherryPick(ref: string, options?: {
    allowConflict?: boolean;
    date?: string;
}): Step;

// @public
export const chipRenderingShowcaseScenario: Scenario;

// @public
export function clearScenarioCache(): Promise<void>;

// @public
export function commit(message: string, options?: {
    date?: string;
}): Step;

// @public
export function conditionally(condition: boolean | ((repo: Parameters<Step>[0]) => boolean | Promise<boolean>), step: Step): Step;

// @public
export function continueCherryPick(): Step;

// @public
export function continueRebase(): Step;

// @public
export function continueRevert(): Step;

// @public
export function createBranch(name: string, options?: {
    from?: string;
}): Step;

// @public
export function createTag(name: string, options?: {
    message?: string;
    sha?: string;
    date?: string;
}): Step;

// @public
export function createTempGitRepo(options?: CreateTempGitRepoOptions): Promise<TempGitRepo>;

// @public (undocumented)
export type CreateTempGitRepoOptions = {
    autoCleanup?: boolean;
    cache?: boolean;
};

// @public
export const danglingCommitScenario: Scenario;

// @public
export function daysAgo(n: number): string;

// @public
export function defineScenario(scenario: Scenario): Scenario;

// @public
export function deleteBranch(name: string, options?: {
    force?: boolean;
}): Step;

// @public
export function deleteFiles(...paths: string[]): Step;

// @public
export function deleteTag(name: string): Step;

// @public
export const detachedHeadScenario: Scenario;

// @public
export const dirtyManyFilesScenario: Scenario;

// @public
export function disableSparseCheckout(): Step;

// @public
export function dropStash(options?: {
    ref?: string;
}): Step;

// @public
export function emptyCommit(message: string, options?: {
    date?: string;
}): Step;

// @public
export const emptyRepoScenario: Scenario;

// @public
export function enableSparseCheckout(paths: string[], options?: {
    cone?: boolean;
}): Step;

// @public
export const featureBranchOneCommitScenario: Scenario;

// @public
export const featurePrReadyScenario: Scenario;

// @public
export type FileMap = Record<string, string>;

// @public
export function findRegistered(name: string): Scenario | undefined;

// @public
export function findRegisteredByTag(tags: string[], match?: 'any' | 'all'): readonly Scenario[];

// @public
export function findScenario(name: string): Scenario | undefined;

// @public
export function findScenariosByTag(tags: string[], match?: 'any' | 'all'): readonly Scenario[];

// @public
export function fromScenario(name: string, ...extraSteps: Step[]): Promise<TempGitRepo>;

// @public (undocumented)
export function fromScenario(name: string, options: FromScenarioOptions, ...extraSteps: Step[]): Promise<TempGitRepo>;

// @public
export type FromScenarioOptions = CreateTempGitRepoOptions;

// @public
export function gitClean(options?: {
    directories?: boolean;
    force?: boolean;
    ignored?: boolean;
}): Step;

// @public
export class GitCommandError extends GitScenariosError {
    constructor(opts: {
        command: string;
        exitCode: number;
        stderr: string;
        atomName?: string;
    });
    // (undocumented)
    readonly atomName?: string;
    // (undocumented)
    readonly command: string;
    // (undocumented)
    readonly exitCode: number;
    // (undocumented)
    readonly stderr: string;
}

// @public
export type GitHookName = 'applypatch-msg' | 'commit-msg' | 'post-checkout' | 'post-commit' | 'post-merge' | 'post-receive' | 'post-update' | 'pre-applypatch' | 'pre-commit' | 'pre-merge-commit' | 'pre-push' | 'pre-rebase' | 'pre-receive' | 'prepare-commit-msg' | 'update';

// @public
export class GitScenariosError extends Error {
    constructor(message: string, code: string);
    // (undocumented)
    readonly code: string;
}

// @public
export type HeadSnapshot = {
    branch: string | null;
    detached: boolean;
    sha: string | null;
};

// @public
export type InProgressOperation = 'merge' | 'rebase' | 'cherry-pick' | 'revert' | 'bisect' | null;

// @public (undocumented)
export function insideSubmodule(submodulePath: string, step: Step): Step;

// @public
export function installHook(name: GitHookName, script: string): Step;

// @public
export const interactiveRebaseMidEditScenario: Scenario;

// @public
export class InvalidArgumentError extends GitScenariosError {
    constructor(opts: {
        parameterName: string;
        constraint: string;
        atomName?: string;
    });
    // (undocumented)
    readonly atomName?: string;
    // (undocumented)
    readonly constraint: string;
    // (undocumented)
    readonly parameterName: string;
}

// @public
export const largeRepoScenario: Scenario;

// @public
export function listRegistered(): readonly Scenario[];

// @public (undocumented)
export const lockedWorktreeScenario: Scenario;

// @public
export function lockWorktree(path: string, options?: {
    reason?: string;
}): Step;

// @public
export const mergeNoConflictScenario: Scenario;

// @public
export const midBisectScenario: Scenario;

// @public
export const midCherryPickConflictScenario: Scenario;

// @public
export const midMergeConflictScenario: Scenario;

// @public
export const midRebaseConflictScenario: Scenario;

// @public
export const midRevertConflictScenario: Scenario;

// @public
export const monorepoMultiPackageScenario: Scenario;

// @public
export const multiCommitBranchScenario: Scenario;

// @public
export const multipleWorktreesScenario: Scenario;

// @public
export const multiRemoteWithTrackingScenario: Scenario;

// @public
export function onBranch(name: string, step: Step): Step;

// @public
export const orphanBranchScenario: Scenario;

// @public
export const outOfDateSubmoduleScenario: Scenario;

// @public
export const partialStageScenario: Scenario;

// @public
export function pinSubmodule(path: string, sha: string): Step;

// @public
export function popStash(options?: {
    ref?: string;
}): Step;

// @public
export function registerScenario(scenario: Scenario): void;

// @public
export function registerScenarios(scenarios: Scenario[]): void;

// @public
export function removeHook(name: GitHookName): Step;

// @public
export function removeNote(options?: {
    ref?: string;
    namespace?: string;
}): Step;

// @public
export function removeRemote(name: string): Step;

// @public
export function removeWorktree(path: string, options?: {
    force?: boolean;
}): Step;

// @public
export function renameFile(from: string, to: string): Step;

// @public
export function renameRemote(from: string, to: string): Step;

// @public
export function repeat(n: number, factory: (index: number) => Step): Step;

// @public
export class RepoAssertion implements PromiseLike<RepoSnapshot> {
    constructor(source: SnapshotSource);
    ahead(n: number): this;
    aheadOf(ref: string, n: number): this;
    behind(n: number): this;
    behindOf(ref: string, n: number): this;
    cleanWorktree(): this;
    commitCount(n: number): this;
    detached(): this;
    dirtyWorktree(): this;
    hasBranch(name: string): this;
    hasConflict(path?: string): this;
    hasModified(path?: string): this;
    hasStaged(path?: string): this;
    hasStash(n?: number): this;
    hasUntracked(path?: string): this;
    inOperation(op: InProgressOperation): this;
    inSyncWithUpstream(): this;
    noConflicts(): this;
    noOperation(): this;
    onBranch(name: string): this;
    // (undocumented)
    then<TResult1 = RepoSnapshot, TResult2 = never>(onfulfilled?: ((value: RepoSnapshot) => TResult1 | PromiseLike<TResult1>) | null, onrejected?: ((reason: unknown) => TResult2 | PromiseLike<TResult2>) | null): PromiseLike<TResult1 | TResult2>;
}

// @public
export class RepoAssertionError extends GitScenariosError {
    constructor(opts: {
        assertion: string;
        expected: unknown;
        actual: unknown;
        message: string;
    });
    // (undocumented)
    readonly actual: unknown;
    // (undocumented)
    readonly assertion: string;
    // (undocumented)
    readonly expected: unknown;
}

// @public
export type RepoSnapshot = {
    head: HeadSnapshot;
    branches: string[];
    status: StatusSnapshot;
    commitCount: number;
    operation: InProgressOperation;
    conflicts: string[];
    stashes: number;
    graph: string[];
};

// @public
export function resetBisect(): Step;

// @public
export const resetRecoverableHeadScenario: Scenario;

// @public
export function resetRegistry(): void;

// @public
export function resetTo(options: {
    target: string;
    mode?: 'soft' | 'mixed' | 'hard';
}): Step;

// @public
export function revert(ref: string, options?: {
    mainline?: number;
    allowConflict?: boolean;
    date?: string;
}): Step;

// @public
export const richHistoryGraphScenario: Scenario;

// @public
export type Scenario = {
    name: string;
    summary: string;
    description: string;
    kind: ScenarioKind;
    tags?: string[];
    setup: (repo: TempGitRepo) => Promise<void>;
    contracts?: string[];
    version?: string;
};

// @public
export type ScenarioKind = 'branch' | 'worktree' | 'operation' | 'history' | 'stash' | 'submodule';

// @public
export class ScenarioNotFoundError extends GitScenariosError {
    constructor(scenarioName: string, availableScenarios: string[]);
    // (undocumented)
    readonly availableScenarios: string[];
    // (undocumented)
    readonly scenarioName: string;
}

// @public
export function seededFiles(opts: {
    files: SeededFileSpec[];
    seed: number;
}): Step;

// @public
export type SeededFileSpec = {
    path: string;
    tokens: number;
    seedOffset?: number;
};

// @public
export function setConfig(key: string, value: string, options?: {
    unset?: boolean;
}): Step;

// @public
export function setRemoteRef(remote: string, branch: string, sha: string): Step;

// @public
export function setUpstream(localBranch: string, remote: string, remoteBranch?: string): Step;

// @public
export function shallowAt(depth: number): Step;

// @public
export const shallowCloneScenario: Scenario;

// @public
export const signedCommitsRequiredScenario: Scenario;

// @public
export const singleStagedFileScenario: Scenario;

// @public
export function snapshotRepo(git: SimpleGit): Promise<RepoSnapshot>;

// @public
export type SnapshotSource = {
    snapshot: () => Promise<RepoSnapshot>;
} | SimpleGit;

// @public
export function spinUpScenario(name: string, options?: SpinUpScenarioOptions): Promise<TempGitRepo>;

// @public
export type SpinUpScenarioOptions = CreateTempGitRepoOptions & {
    remote?: string;
};

// @public
export function stageFiles(...paths: string[]): Step;

// @public
export function startBisect(options: {
    bad: string;
    good: string;
}): Step;

// @public
export function startInteractiveRebase(onto: string): Step;

// @public
export function startMerge(branch: string, options?: {
    allowConflict?: boolean;
    noFastForward?: boolean;
    squash?: boolean;
    date?: string;
    message?: string;
}): Step;

// @public
export function startRebase(onto: string, options?: {
    allowConflict?: boolean;
}): Step;

// @public
export function stashChanges(options?: {
    message?: string;
    includeUntracked?: boolean;
    keepIndex?: boolean;
}): Step;

// @public
export const stashedChangesScenario: Scenario;

// @public
export const stashWithUntrackedScenario: Scenario;

// @public
export type StatusSnapshot = {
    clean: boolean;
    staged: string[];
    modified: string[];
    untracked: string[];
    ahead: number;
    behind: number;
};

// @public
export type Step = (repo: TempGitRepo) => Promise<void>;

// @public
export const submoduleWithHistoryScenario: Scenario;

// @public
export function switchToBranch(name: string, options?: {
    from?: string;
}): Step;

// @public
export type TempGitRepo = {
    path: string;
    git: SimpleGit;
    writeFile: (filePath: string, content: string) => Promise<void>;
    readFile: (filePath: string) => Promise<string>;
    exists: (filePath: string) => Promise<boolean>;
    commitAll: (message: string) => Promise<void>;
    snapshot: () => Promise<RepoSnapshot>;
    cleanup: () => Promise<void>;
};

// @public
export const twoCommitFeatureScenario: Scenario;

// @public
export function unlockWorktree(path: string): Step;

// @public
export function unregisterScenario(name: string): boolean;

// @public
export function unshallow(): Step;

// @public
export function unstageFiles(...paths: string[]): Step;

// @public
export function withAuthor(identity: AuthorIdentity, step: Step): Step;

// @public
export function withRemoteTracking(remote: string, branch: string, step: Step): Step;

// @public
export function writeFiles(files: FileMap): Step;

// @public
export function writeGitattributes(rules: string[] | string): Step;

// @public
export function writeGitignore(patterns: string[] | string): Step;

// (No @packageDocumentation comment for this package)

```
