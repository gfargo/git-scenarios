---
/**
 * Vercel Analytics component — injects the analytics script on every page.
 * Custom events can be tracked via the `track()` function from @vercel/analytics.
 */
---

<script>
  import { inject } from '@vercel/analytics'
  inject()
</script>
