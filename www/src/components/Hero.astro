---
/**
 * Suppressed Starlight Hero — the splash landing page now renders its
 * own custom hero (HeroPlumbing.astro) inside the markdown body. The
 * built-in hero would double up the page title and a stray container,
 * so we render nothing at all.
 *
 * The page <title> + meta description still come from the page's
 * frontmatter, just rendered in <head> rather than as an h1 inside
 * the content panel.
 */
---
